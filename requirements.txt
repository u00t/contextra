fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pandas>=2.2
numpy>=1.26
sqlalchemy>=2.0
python-dotenv>=1.0

# Optional — only needed for the Postgres connector
# psycopg2-binary>=2.9

# Optional — only needed to enable LLM-backed semantic enrichment
# openai>=1.0
